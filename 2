#include <iostream>
#include <iomanip>  
using namespace std;
void swap(double *a, double *b) {
    double temp = *a;
    *a = *b;
    *b = temp;
}
int main() {
    double x, y;
    cin >> x >> y; 
    swap(&x, &y); 
    cout << fixed << setprecision(2) << x << " " << y << endl;
    return 0;
}
